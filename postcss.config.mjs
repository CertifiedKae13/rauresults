// The dashboard uses its own CSS in app/globals.css. Keeping this config empty
// prevents Cloudflare Pages from loading Tailwind's native CSS transformer.
const config = {};

export default config;
